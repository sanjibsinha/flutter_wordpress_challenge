import 'package:flutter/material.dart';

import 'view/news_app.dart';

/// this is first step where we have added custom theme
/// and fetched the title of the posts on our home page

void main() {
  runApp(const NewsApp());
}
